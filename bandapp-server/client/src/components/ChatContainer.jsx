//import socket.io-client


import React from 'react'

const ChatContainer = () => {

    //socket.on('newMessage', (allMessages, newMessage) => {
// update chat container destructuring  to keep the previous messages[...allMessages, newMessage]
// newMessage is an object that has sender, receiver, message and chatId
// send notification to receiver
//})

// handleNewMessage function that keeps track of what is being written
// handleSubmitMessage emits the message to the backend


// Have a text-box to display previous messages
// Have an input to type new messages



  return (
    <div>ChatContainer</div>
  )
}

export default ChatContainer